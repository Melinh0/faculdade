//Escrever um programa que receba um valor inteiro do usuario e apresente o seu valor absoluto(Modulo). não ultilize estrutura de decisão if.

#include <stdio.h>

int main(void) {
  int num;
  printf("Digite o valor do numero a ser lido");
  scanf("%d", &num);
  return 0;
}